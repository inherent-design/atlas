services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: atlas-qdrant
    ports:
      - '6333:6333' # HTTP API
      - '6334:6334' # gRPC API (optional)
    volumes:
      - ./data/qdrant:/qdrant/storage
    environment:
      # Production-ready settings
      - QDRANT__SERVICE__HTTP_PORT=6333
      - QDRANT__SERVICE__GRPC_PORT=6334
      # Optional: Enable persistence
      - QDRANT__STORAGE__WAL_CAPACITY_MB=32
      - QDRANT__STORAGE__WAL_SEGMENTS_AHEAD=0
    # Resource constraints: prevent Qdrant from starving other processes
    deploy:
      resources:
        limits:
          cpus: '0.33'
          memory: 6g    # ~0.42 of 16GB system RAM
        reservations:
          cpus: '0.1'
          memory: 2g
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:6333/metrics']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  ollama:
    image: ollama/ollama:latest
    container_name: atlas-ollama
    ports:
      - '11434:11434' # Ollama API
    volumes:
      - ./data/ollama:/root/.ollama
    environment:
      - OLLAMA_HOST=0.0.0.0:11434
      - OLLAMA_VULKAN=1
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:11434/api/tags']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

volumes:
  qdrant_data:
    driver: local
  ollama_data:
    driver: local
